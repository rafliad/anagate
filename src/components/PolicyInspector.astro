---
import { policyChecks } from "../content/landing";
---

<section class="policy section-shell" id="policy" aria-labelledby="policy-title">
  <div class="section-heading" data-reveal>
    <p class="signal">Policy inspector</p>
    <h2 id="policy-title">A concrete request, checked line by line.</h2>
  </div>
  <div class="inspector" data-reveal style="--reveal-delay: 0.15s" aria-label="Policy inspection example">
    <div class="inspector-request">
      <span>Payment request</span>
      <strong>42 XLM to stellar.tools</strong>
      <p>Reason: settle compute usage for an agent task.</p>
    </div>
    <div class="check-list">
      {policyChecks.map((check) => (
        <article class={`check-row is-${check.result}`}>
          <div>
            <h3>{check.name}</h3>
            <p>{check.detail}</p>
          </div>
          <span>{check.value}</span>
        </article>
      ))}
    </div>
  </div>
</section>
